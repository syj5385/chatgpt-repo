name: Build SmartThings Remote APK

on:
  push:
    branches:
      - smartthings-remote-widget
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore Android project
        shell: bash
        run: |
          cat smartthings-src-part-* > /tmp/smartthings-src.b64
          base64 -d /tmp/smartthings-src.b64 > /tmp/smartthings-src.tgz
          tar -xzf /tmp/smartthings-src.tgz

      - name: Apply v2 widget design patch
        shell: bash
        run: |
          base64 -d smartthings-v2.patch.gz.b64 | gzip -d > /tmp/smartthings-v2.patch
          patch -p2 < /tmp/smartthings-v2.patch

      - name: Apply v3 appliance UI patch
        shell: bash
        run: |
          cat smartthings-v3.patch.gz.b64.part* > /tmp/smartthings-v3.patch.gz.b64
          base64 -d /tmp/smartthings-v3.patch.gz.b64 | gzip -d > /tmp/smartthings-v3.patch
          patch -p1 < /tmp/smartthings-v3.patch

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk SmartThings-Remote-Widget-v3.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: smartthings-remote-widget-v3-apk
          path: SmartThings-Remote-Widget-v3.apk
          if-no-files-found: error
